# Copia este archivo a .env y cambia los valores en producción.
# docker-compose los toma automáticamente (con estos defaults si no existe .env).
DB_USER=postgres
DB_PARK_PASSWORD=password
DB_MICRO_PASSWORD=password
